% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndex_connect.r
\name{ndex.connect}
\alias{ndex.connect}
\title{Connect to NDEx REST API}
\usage{
ndex.connect(username, password, host = "ndexConf$connection$host",
  apiPath = "ndexConf$connection$api", ndexConf = ndex.conf,
  verbose = FALSE)
}
\arguments{
\item{username}{character (optional); username}

\item{password}{character (optional); password}

\item{host}{character (default: "ndexConf$connection$host"); Host address of NDEx server; By default the url set in ndexConf$defaults$connection$host is used. ("http://www.ndexbio.org")}

\item{apiPath}{character (default: "ndexConf$connection$api"); URL path of the REST api; By default the url set in ndexConf$defaults$connection$api is used. ("http://www.ndexbio.org")}

\item{ndexConf}{config object (nested list, default: ndex.conf); Configuration of NDEx REST server; Set in ndex.conf (set in ndex_api_config.r or ndex_api_config.yml): It contains specifications for NDEx server api version 1.3 and 2.0. The default api is specified by 'defaultVersion'}

\item{verbose}{logical (optional); whether to print out extended feedback}
}
\value{
returns object of class NDEXConnection which stores options, authentication and api configuration if successfull, NULL otherwise
}
\description{
This function creates an NDEXConnection which stores options and authentication details. It is a parameter required for most of the other ndexr functions.
If username and password are missing an anonymous connection is created, which already offers most of the retrieval functionality.
}
\examples{
ndexcon = ndex.connect()   ## log in anonymously
ndexcon = ndex.connect(verbose=TRUE)   ## same as above with extended feedback
\dontrun{
ndexcon = ndex.connect('user','password')   ## log in with credentials
ndexcon = ndex.connect(host='localhost:8765')   ## running some NDEx server locally
ndexcon = ndex.connect(ndexConf=ndex.conf$Version_2.0)   ## manually change the api and connection configuration
}
}
\seealso{
\code{\link{ndex.conf}}, \code{\link{updateConfigFromYaml}}
}
